# Configuración del Sistema ABICK - Base de Datos Dual
# Copiar este archivo como .env en la raíz del proyecto

# Configuración del Servidor
PORT=3001
NODE_ENV=development

# Configuración Modbus TCP
PLC_IP=192.168.0.253
PLC_PORT=502

# Configuración Base de Datos MySQL Local (ReadConnection)
# Esta es la base de datos principal para operaciones del sistema
DB_HOST=127.0.0.1
DB_USER=root
DB_PASSWORD=
DB_NAME=db_oxigeno

# Configuración Base de Datos MySQL Remota (ReadConnectionHost)
# Esta es la base de datos para sincronización y respaldo
DB_HOST_REMOTE=138.255.103.114
DB_USER_REMOTE=svsmarte_svsmarte
DB_PASSWORD_REMOTE=xN88ZfEp3t*(Y0
DB_NAME_REMOTE=svsmarte_db_oxigeno

# Configuración Email
EMAIL_HOST=mail.smarteyes.cl
EMAIL_PORT=587
EMAIL_USER=alertas@smarteyes.cl
EMAIL_PASS=Smart2000!

# Configuración WebSocket
WS_PORT=3002

# NOTAS IMPORTANTES:
# 1. La base de datos local debe estar instalada y funcionando
# 2. La base de datos remota se conecta automáticamente si está disponible
# 3. Si falla la conexión remota, el sistema continúa solo con la base local
# 4. La sincronización se realiza cada 60 segundos automáticamente
